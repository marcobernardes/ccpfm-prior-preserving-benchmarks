# Smoke-test status

The package passed `python -m compileall` and a reduced one-trial execution at `h=0.20` on the circle for the smooth variable-coefficient problem.

The reduced run is only an implementation check. Its numerical values must not be copied into the manuscript. Run the full refinement/trial command in `README.md` to generate publication tables and figures.
